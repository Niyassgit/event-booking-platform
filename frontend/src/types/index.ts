export interface LoginPayload {
  email: string;
  password: string;
}

export interface signupPayLoad {
  email: string;
  name: string;
  password: string;
  cpassword: string;
  role: string;
}
